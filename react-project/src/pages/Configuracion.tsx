import { Typography, Box } from '@mui/material';
export default function Configuracion() {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>Configuración</Typography>
            <Typography>Configure las opciones del sistema aquí.</Typography>
        </Box>
    );
} 